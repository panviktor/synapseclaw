# Config Reference

Main config lives under `~/.synapseclaw/config.toml`. Helper agents use `~/.synapseclaw/agents/*/config.toml`, and secrets should be supplied through the local service environment.

Important Skills-related config areas include auto-promotion policy, open-skills sources, workspace package porting, and embedding provider behavior. Auto-promotion should be explicit policy, not a hidden background write.

