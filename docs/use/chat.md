# Chat

In chat, you give the agent a task and it may call tools before answering. The runtime handles provider context, tool execution, and memory bookkeeping behind the scenes.

For now, treat chat as the simplest way to drive the agent and verify behavior. Stable runtime commands are documented where they exist, especially the `/skills` commands in the Skills guide.

